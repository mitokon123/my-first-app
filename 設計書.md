# 設計書（モンスター育成 × ローグライク）ver.2

本ドキュメントは実装前の設計です。内容をご確認いただき、問題がなければ実装を開始します。
ゲームバランス・数値・ゲーム性に関わる決定はすべてプレイヤー（あなた）が持ちます。本書は「実装方法の提案」です。

> ver.2 の変更点（あなたの指示を反映）
> - 起動方法は **(B) `<script>`方式**（ダブルクリック起動優先）。名前空間＋IIFEでグローバル汚染を回避。
> - 操作は **WASD と 矢印キーの両対応**。
> - MVPから **仮のドット絵／手描き風スプライト素材**を使用し、後から差し替えやすい構造にする。
> - **CaptureSystem を BattleSystem から独立**（フィールド/イベント捕獲も見据える）。
> - **DataLoader は読み込み専用**にし、**GameData** がデータ管理・参照窓口を担う。
> - **abilities（特性・パッシブ）** を追加（初期は空でも可）。
> - **Monster を MonsterData（種族/図鑑）と MonsterInstance（所持個体）に分離**。

---

## 0. 方針の再確認

- 使用技術は **HTML / CSS / JavaScript のみ**（フレームワーク・ライブラリなし、ES6以降）。
- 画面描画は **Canvas API**。HTML はエントリーポイントとして最小限。
- **数値はコードに書かず、`data/` 以下のデータファイルで管理**（HP・攻撃・出現率・捕獲率・スキル威力など）。
- モンスター・スキル・アイテム・敵・ボス・特性は **データ駆動**。追加はデータファイル編集だけで対応。
- **機能ごとにファイル分割**。**唯一のグローバル名前空間 `MyGame`** の下に、各ファイルを **IIFE** で登録し、グローバル汚染を避ける。
- 小さな機能単位で、毎回動作確認できる状態を保って進める。

---

## 1. ディレクトリ構成

```
my-first-app/
├── app.html                # エントリーポイント（最小限のHTML + Canvas + script読み込み）
├── README.md               # 構成・起動方法・ファイルの役割・開発方針（常に最新化）
├── 設計書.md               # 本書
├── css/
│   └── style.css
├── assets/                 # ★仮素材（差し替え可能）
│   └── sprites/            # ドット絵／手描き風PNG（player, slime, tiles など）
├── data/                   # ★データ駆動の中心（(B)方式のため .js で名前空間に登録）
│   ├── config.js           # 画面サイズ、タイルサイズ、パーティ上限など
│   ├── monsters.js         # MonsterData（種族/図鑑データ）
│   ├── skills.js           # スキル定義
│   ├── items.js            # アイテム定義
│   ├── abilities.js        # ★特性・パッシブ（初期は空でOK）
│   ├── enemies.js          # エンカウント設定
│   ├── bosses.js           # ボス定義
│   ├── dungeon.js          # ダンジョン生成パラメータ
│   └── sprites.js          # スプライト定義（id → 画像パス）差し替え容易化
└── js/
    ├── namespace.js        # ★唯一のグローバル: window.MyGame = {} を定義
    ├── main.js             # 起動・初期化のみ
    ├── core/
    │   ├── Game.js         # 全体統括・シーン切替の入口
    │   ├── GameLoop.js     # requestAnimationFrame ループ
    │   ├── SceneManager.js # シーン管理
    │   ├── Input.js        # 入力抽象化（WASD + 矢印キー両対応）
    │   ├── DataLoader.js   # ★読み込み専用：登録済み生データを集約してGameDataへ渡す
    │   ├── GameData.js     # ★データ管理・参照窓口（GameData.monsters 等）
    │   └── AssetLoader.js  # ★画像スプライトの読み込み・保持
    ├── scenes/
    │   ├── TitleScene.js
    │   ├── DungeonScene.js
    │   ├── BattleScene.js
    │   └── EndingScene.js
    ├── dungeon/
    │   ├── DungeonGenerator.js
    │   └── Dungeon.js
    ├── entities/
    │   ├── Player.js
    │   ├── MonsterData.js      # ★種族データの参照ラッパ（GameData経由で取得）
    │   ├── MonsterInstance.js  # ★プレイヤーが所持する個体
    │   └── Party.js
    ├── battle/
    │   └── BattleSystem.js     # ターン制戦闘（捕獲は含まない）
    ├── capture/
    │   └── CaptureSystem.js    # ★戦闘から独立。捕獲判定の責務のみ
    ├── systems/
    │   └── SaveManager.js      # localStorage セーブ/ロード
    ├── render/
    │   ├── Renderer.js         # Canvas描画の共通ユーティリティ
    │   └── SpriteRenderer.js   # スプライト描画（素材差し替えを吸収）
    └── ui/
        └── HUD.js
```

※「完成時の目標構成」です。実際は MVP の機能単位で少しずつ追加します。

---

## 2. 主要クラスと役割

| クラス | 役割 |
|--------|------|
| `Game` | 全体統括。各マネージャを保持し、シーン切替の入口。 |
| `GameLoop` | 毎フレーム `update(dt)` → `render(ctx)` を回す。 |
| `SceneManager` | 現在シーンの保持と切替。 |
| `Input` | **WASD / 矢印キー両対応**。押下中・押した瞬間を管理。 |
| `DataLoader` | **読み込み専用**。`data/*.js` が登録した生データを集約し `GameData` を生成して返すだけ（以後は保持しない）。 |
| `GameData` | **データ管理・参照窓口**。`GameData.monsters` `GameData.skills` `GameData.items` `GameData.abilities` などを提供。`getMonsterData(id)` 等のアクセサを持つ。 |
| `AssetLoader` | スプライト画像を読み込み・保持し、id で取得できるようにする。 |
| `DungeonGenerator` / `Dungeon` | ランダム生成 / マップ保持・当たり判定。 |
| `Player` | プレイヤーの位置・向き・所持パーティ。 |
| `MonsterData` | **種族/図鑑データ**（名前・基本ステータス・覚える技・進化情報・出現情報・特性）。`GameData` から取得する不変データの参照。 |
| `MonsterInstance` | **所持する個体**（現在レベル・現在HP・経験値・覚えている技・性格・個体差・装備・状態異常）。種族は `MonsterData` を id 参照。 |
| `Party` | 手持ち `MonsterInstance` の並び・上限管理。 |
| `BattleSystem` | ターン制戦闘の進行（行動順・ダメージ計算・勝敗）。**捕獲は扱わない**。 |
| `CaptureSystem` | **独立した捕獲責務**。捕獲成功判定（捕獲率はデータ参照）。戦闘中に限らずフィールド/イベントからも呼べる形。 |
| `SaveManager` | `localStorage` へセーブ/ロード。 |
| `Renderer` / `SpriteRenderer` / `HUD` | 描画補助・スプライト描画・UI描画。 |

**責務分離**：ロジック（System/Entity）と描画（Renderer系）を分け、データは **GameData 経由でのみ**参照する。

---

## 3. データ構造の例

> (B) 方式では `fetch` で `.json` を読めない環境がある（後述 §7）ため、各データは **`.js` で名前空間に登録**します。中身は JSON と同じオブジェクト構造で、「データファイルだけ編集すれば拡張できる」性質は保ちます。

`data/monsters.js`（MonsterData：種族/図鑑データ。数値は仮）
```js
(function (NS) {
  NS.rawData = NS.rawData || {};
  NS.rawData.monsters = {
    slime: {
      id: "slime",
      name: "スライム",
      baseHp: 20, baseAttack: 5, baseDefense: 3,
      captureRate: 0.4,          // 捕獲率
      spawnRate: 0.5,            // 出現率
      learnset: [{ level: 1, skill: "tackle" }], // 覚える技
      evolvesTo: null,           // 進化情報（例: { to:"kingSlime", level:12 }）
      abilities: [],             // 特性id（abilities.js を参照）
      expReward: 8,
      sprite: "slime"            // sprites.js のスプライトid
    }
  };
})(window.MyGame);
```

`data/abilities.js`（特性・パッシブ。初期は空でOK）
```js
(function (NS) {
  NS.rawData = NS.rawData || {};
  NS.rawData.abilities = {
    // 例（将来）: guts: { name:"こんじょう", trigger:"onLowHp", effect:"attackUp" }
  };
})(window.MyGame);
```

`data/sprites.js`（素材差し替えを吸収：id → 画像パス）
```js
(function (NS) {
  NS.rawData = NS.rawData || {};
  NS.rawData.sprites = {
    player: "assets/sprites/player.png",
    slime:  "assets/sprites/slime.png",
    tiles:  "assets/sprites/tiles.png"
  };
})(window.MyGame);
```

**MonsterData と MonsterInstance の関係**
```
monsters.js（種族データ）  ──参照──▶  MonsterInstance（所持個体）
   例: slime の baseHp=20                 例: レベル7のスライム, 現在HP15, exp120,
                                            性格=がんばりや, 個体値+2, 装備=なし
```
`MonsterInstance` は `speciesId`（例 "slime"）で `MonsterData` を参照し、レベル・個体差から実効ステータスを算出します。育成・進化・レア個体はこの分離を土台に拡張します。

---

## 4. ゲームの更新ループ

```
app.html が以下を順に読み込む:
  namespace.js → data/*.js（生データ登録）→ js/**（クラス定義）→ main.js

main.js:
  const raw = MyGame.rawData;              // データファイルが登録済み
  const gameData = DataLoader.build(raw);  // 読み込み専用：生データ→GameData生成
  await AssetLoader.load(gameData.sprites);// スプライト画像の読み込み完了を待つ
  const game = new MyGame.Game(canvas, gameData, assets);
  game.start();                            // GameLoop開始

毎フレーム loop(timestamp):
  dt = timestamp - last
  Input.update()
  SceneManager.current.update(dt)
  SceneManager.current.render(ctx)
  Input.lateUpdate()      // 「押した瞬間」フラグのリセット
  requestAnimationFrame(loop)
```

- 論理更新と描画を分離。シーンごとに `update`/`render` を実装。
- データは `GameData`、画像は `AssetLoader` から参照。`DataLoader` はループ中に登場しない。

---

## 5. ファイル間の依存関係（概略）

```
app.html → namespace.js, data/*.js, js/**, main.js
main.js  → DataLoader(build) → GameData,  AssetLoader,  core/Game
Game     → GameLoop, SceneManager, Input, systems/SaveManager
SceneManager → scenes/*
DungeonScene → dungeon/*, entities/Player, render/*, ui/HUD
BattleScene  → battle/BattleSystem, capture/CaptureSystem, entities/MonsterInstance, entities/Party, ui/HUD
各System/Entity → GameData（データ参照）, AssetLoader（画像参照）
```

- 依存は「上位（Game/Scene）→ 下位（System/Entity/Render）」の一方向。循環依存を避ける。
- **CaptureSystem は BattleSystem に依存しない**（戦闘・フィールド・イベントのどこからでも呼べる独立モジュール）。

---

## 6. MVP の実装順（提案）

毎回動作確認できる状態で、1ステップずつ区切って確認いただけます。

1. **土台**：namespace.js + app.html + Canvas 表示 + GameLoop（画面に「Loading/Title」表示）
2. **データ基盤**：config.js + DataLoader(build) + GameData（データ参照の確立）
3. **素材基盤**：sprites.js + AssetLoader（仮スプライト1枚を描画）
4. **ダンジョン表示**：固定マップをタイル描画
5. **プレイヤー移動**：WASD/矢印で移動＋壁当たり判定
6. **ランダムダンジョン生成**
7. **戦闘（基本）**：エンカウント→ターン制→勝敗（MonsterInstance使用）
8. **モンスター捕獲**（独立CaptureSystem）
9. **パーティ編成**
10. **セーブ／ロード**
11. **ボス1体 → エンディング**

---

## 7. 実装前に確認したい点（(B)選択に伴う技術メモ）

**(B) ダブルクリック起動と JSON について。**
ブラウザは `file://`（ダブルクリックで開いた状態）では、セキュリティ制限により **`fetch`/`XMLHttpRequest` でローカルの `.json` を読み込めません**（多くのブラウザでブロックされます）。一方 **`<script>` 読み込みは可能**です。

そこで本設計では、データを **`data/*.json` ではなく `data/*.js`（名前空間へ登録する形）** としました。
- 中身は JSON と同じオブジェクト構造で、**「データファイルだけ編集すれば追加・変更できる」性質は維持**されます。
- 将来ローカルサーバ/モジュール化へ移行する際も、`.js` → `.json` への変換は容易です。

この方針（データを `.js` で持つ）で問題なければ、**ステップ1（土台）**から実装を開始します。
「どうしても拡張子は `.json` にしたい」等のご希望があれば、代替案（ビルド不要の簡易ローカルサーバ併用など）もご提案します。
```
